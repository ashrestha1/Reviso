import React from 'react'
import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  topLevel: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  paddedElement: { padding: 20 }
})
